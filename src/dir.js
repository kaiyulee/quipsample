var React = require('react');
var Item = require('./item');
var Dir = React.createClass({
    render: function() {
        return (
            <div className = "dir">
                <ul className = "list">
                    return <Item />
                </ul>
            </div>
        );
    }
});
module.exports = Dir;
